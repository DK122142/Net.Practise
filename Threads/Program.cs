using System;
using System.Collections.Generic;
using System.Linq;
using System.Threading;

namespace Threads
{
    class Program
    {
        static void Main(string[] args)
        {
            //Task1
            // Tasks.CreateArray();
            //Task2
            // Tasks.SubArray(Tasks.CreateArray(), 100, 200);
            //Task3
            // Tasks.MinInArray(Tasks.CreateArray());
            //Task4
            // Tasks.AverageInArray(Tasks.CreateArray());
        }
    }
}
